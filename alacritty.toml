[key_bindings]
  # Ctrl+E: Move cursor to the end of the line
  { key = "E", mods = "Control", action = "MoveToLineEnd" }
  
  # Ctrl+A: Move cursor to the beginning of the line
  { key = "A", mods = "Control", action = "MoveToLineStart" }
